package com.qiandao.jedis;

/**
 * @Author: shangxunchao
 * @Description:
 * @Date: Created in 2019/8/29 12:05
 */
public class Demo {
/*	public StudentStatus getStudentStatusByQcToken(String qcToken) {
		StudentStatus ss = (StudentStatus) redisTemplate.opsForValue().get(qcToken);
		if (ss != null) {
			return ss;
		}
		ss = studentStatusMapper.getStudentStatusByQcToken(qcToken);
		if (ss != null) {
			redisTemplate.opsForValue().set(qcToken, ss, RedisExpireConstant.REDIS_EXPIRE_TEN_MIN, TimeUnit.SECONDS);
		}
		return ss;
	}*/
}
